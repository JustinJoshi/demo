<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>InvoiceIQ - Add Data Source</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #3b82f6;
            --primary-hover: #2563eb;
            --secondary: #8b5cf6;
            --background: #0f172a;
            --surface: #1e293b;
            --text-primary: #f8fafc;
            --text-secondary: #cbd5e1;
            --border: #334155;
            --success: #10b981;
            --warning: #f59e0b;
            --error: #ef4444;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--background);
            color: var(--text-primary);
            overflow-x: hidden;
        }

        .app-container {
            display: flex;
            min-height: 100vh;
            width: 100%;
        }

        /* Sidebar */
        .sidebar {
            width: 16rem;
            background: var(--surface);
            border-right: 1px solid var(--border);
            padding: 1.5rem;
            position: fixed;
            height: 100vh;
            overflow-y: auto;
            flex-shrink: 0;
        }

        .sidebar-header {
            margin-bottom: 2rem;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-size: 1.5rem;
            font-weight: 700;
        }

        .logo-icon {
            font-size: 2rem;
        }

        .sidebar-nav {
            display: flex;
            flex-direction: column;
            gap: 2rem;
        }

        .nav-section-title {
            font-size: 0.75rem;
            font-weight: 600;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 0.05em;
            margin-bottom: 0.5rem;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            border-radius: 0.5rem;
            color: var(--text-secondary);
            text-decoration: none;
            transition: all 0.2s ease;
            font-size: 0.875rem;
        }

        .nav-item:hover {
            background: var(--background);
            color: var(--text-primary);
        }

        .nav-item.active {
            background: var(--primary);
            color: white;
        }

        /* Main Content */
        .main-content {
            flex: 1;
            margin-left: 16rem;
            width: calc(100% - 16rem);
        }

        .top-nav {
            background: var(--surface);
            border-bottom: 1px solid var(--border);
            padding: 1rem 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .breadcrumb {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--text-secondary);
            font-size: 0.875rem;
        }

        .top-nav-right {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .search-input {
            padding: 0.5rem 1rem;
            background: var(--background);
            border: 1px solid var(--border);
            border-radius: 0.5rem;
            color: var(--text-primary);
            font-size: 0.875rem;
            width: 15rem;
        }

        .search-input:focus {
            outline: none;
            border-color: var(--primary);
        }

        .user-menu {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .user-avatar {
            width: 2rem;
            height: 2rem;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
        }

        /* Page Content */
        .page-content {
            padding: 2rem;
            max-width: 100%;
        }

        .page-header {
            margin-bottom: 2rem;
        }

        .page-header h1 {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .page-header p {
            color: var(--text-secondary);
        }

        .alert {
            padding: 1rem;
            border-radius: 0.75rem;
            margin-bottom: 1.5rem;
            display: none;
            align-items: center;
            gap: 0.5rem;
        }

        .alert-success {
            background: rgba(16, 185, 129, 0.1);
            border: 1px solid var(--success);
            color: var(--success);
        }

        /* Chart Panel (Manual Entry) */
        .chart-panel {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 1rem;
            padding: 1.5rem;
            margin-bottom: 2rem;
        }

        .panel-header {
            margin-bottom: 1.5rem;
        }

        .panel-title {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        /* Forms */
        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            font-size: 0.875rem;
            color: var(--text-secondary);
        }

        .form-input, .form-select {
            width: 100%;
            padding: 0.75rem 1rem;
            background: var(--background);
            border: 1px solid var(--border);
            border-radius: 0.5rem;
            color: var(--text-primary);
            font-size: 0.875rem;
            transition: all 0.2s ease;
        }

        .form-input:focus, .form-select:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
        }

        .form-textarea {
            width: 100%;
            padding: 0.75rem 1rem;
            background: var(--background);
            border: 1px solid var(--border);
            border-radius: 0.5rem;
            color: var(--text-primary);
            font-size: 0.875rem;
            min-height: 8rem;
            font-family: 'Inter', monospace;
            resize: vertical;
        }

        .form-textarea:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
        }

        .form-help {
            font-size: 0.75rem;
            color: var(--text-secondary);
            margin-top: 0.5rem;
        }

        .checkbox-group {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .checkbox-group input[type="checkbox"] {
            width: 1.25rem;
            height: 1.25rem;
            cursor: pointer;
        }

        .btn {
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 0.5rem;
            font-weight: 600;
            font-size: 0.875rem;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-1px);
            box-shadow: 0 6px 16px rgba(59, 130, 246, 0.4);
        }

        .btn-secondary {
            background: var(--surface);
            color: var(--text-primary);
            border: 1px solid var(--border);
        }

        .btn-secondary:hover {
            background: var(--border);
        }

        .btn-group {
            display: flex;
            gap: 1rem;
        }

        /* Info Box */
        .info-box {
            background: rgba(59, 130, 246, 0.05);
            border: 1px solid rgba(59, 130, 246, 0.2);
            border-radius: 1rem;
            padding: 1.5rem;
            margin-bottom: 2rem;
        }

        .info-box h3 {
            font-size: 1.125rem;
            margin-bottom: 1rem;
        }

        .info-box ul {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .info-box li {
            color: var(--text-secondary);
            padding-left: 1.5rem;
            position: relative;
        }

        .info-box li::before {
            content: '→';
            position: absolute;
            left: 0;
            color: var(--primary);
        }

        /* Source Grid */
        .source-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(18rem, 1fr));
            gap: 1.5rem;
        }

        .source-card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 1rem;
            padding: 1.5rem;
            cursor: pointer;
            transition: all 0.2s ease;
            position: relative;
        }

        .source-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
            border-color: var(--primary);
        }

        .source-icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }

        .source-card h3 {
            font-size: 1.25rem;
            margin-bottom: 0.5rem;
        }

        .source-card p {
            color: var(--text-secondary);
            font-size: 0.875rem;
            line-height: 1.6;
        }

        .badge {
            position: absolute;
            top: 1rem;
            right: 1rem;
            padding: 0.25rem 0.75rem;
            border-radius: 9999px;
            font-size: 0.75rem;
            font-weight: 600;
        }

        .badge-popular {
            background: rgba(59, 130, 246, 0.1);
            color: var(--primary);
        }

        .badge-new {
            background: rgba(16, 185, 129, 0.1);
            color: var(--success);
        }

        /* Modal */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            z-index: 1000;
            align-items: center;
            justify-content: center;
            padding: 2rem;
        }

        .modal-content {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 1rem;
            max-width: 40rem;
            width: 100%;
            max-height: 90vh;
            overflow-y: auto;
        }

        .modal-header {
            padding: 1.5rem;
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .modal-title {
            font-size: 1.5rem;
            font-weight: 700;
        }

        .close-btn {
            background: none;
            border: none;
            color: var(--text-secondary);
            font-size: 2rem;
            cursor: pointer;
            line-height: 1;
            padding: 0;
            width: 2rem;
            height: 2rem;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 0.25rem;
            transition: all 0.2s ease;
        }

        .close-btn:hover {
            background: var(--background);
            color: var(--text-primary);
        }

        .modal form {
            padding: 1.5rem;
        }

        /* Responsive */
        @media (max-width: 1024px) {
            .sidebar {
                width: 14rem;
            }

            .main-content {
                margin-left: 14rem;
                width: calc(100% - 14rem);
            }

            .source-grid {
                grid-template-columns: repeat(auto-fill, minmax(15rem, 1fr));
            }
        }

        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
                position: fixed;
            }

            .main-content {
                margin-left: 0;
                width: 100%;
            }

            .top-nav {
                padding: 1rem;
            }

            .search-input {
                width: 10rem;
            }

            .page-content {
                padding: 1rem;
            }

            .source-grid {
                grid-template-columns: 1fr;
            }

            .modal-content {
                max-width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="app-container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="logo">
                    <div class="logo-icon">📊</div>
                    <span>InvoiceIQ</span>
                </div>
            </div>

            <nav class="sidebar-nav">
                <div class="nav-section">
                    <div class="nav-section-title">Main</div>
                    <a href="/dashboard" class="nav-item">
                        <span>🏠</span>
                        <span>Dashboard</span>
                    </a>
                    <!-- <a href="#" class="nav-item">
                        <span>📈</span>
                        <span>Metrics</span>
                    </a>
                    <a href="#" class="nav-item">
                        <span>📊</span>
                        <span>Analytics</span>
                    </a> -->
                </div>

                <div class="nav-section">
                    <div class="nav-section-title">Data Sources</div>
                    <!-- <a href="#" class="nav-item">
                        <span>🔌</span>
                        <span>Connections</span>
                    </a> -->
                    <a href="/addSource" class="nav-item active">
                        <span>➕</span>
                        <span>Add Source</span>
                    </a>
                </div>

                <!-- <div class="nav-section">
                    <div class="nav-section-title">Settings</div>
                    <a href="#" class="nav-item">
                        <span>🔔</span>
                        <span>Alerts</span>
                    </a>
                    <a href="#" class="nav-item">
                        <span>⚙️</span>
                        <span>Settings</span>
                    </a>
                </div> -->
            </nav>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <header class="top-nav">
                <div class="breadcrumb">
                    <span>InvoiceIQ</span>
                    <span>/</span>
                    <span>Data Sources</span>
                    <span>/</span>
                    <span>Add Source</span>
                </div>

                <div class="top-nav-right">
                    <input type="text" class="search-input" placeholder="Search data sources...">
                    <!-- <div class="user-menu">
                        <div class="user-avatar"></div>
                        <span>Alex Chen</span>
                    </div> -->
                </div>
            </header>

            <div class="page-content">
                <div class="page-header">
                    <h1>Add Data Source</h1>
                    <p>Connect external services to track and visualize your personal metrics</p>
                </div>

                <div id="successAlert" class="alert alert-success">
                    ✅ Data source connected successfully!
                </div>

                <div id="manualEntryAlert" class="alert alert-success">
                    ✅ Manual data entry added successfully!
                </div>

                <!-- Manual Entry Section -->
                <div class="chart-panel" style="margin-bottom: 3rem;">
                    <div class="panel-header">
                        <h3 class="panel-title">✏️ Quick Manual Entry</h3>
                        <p style="color: var(--text-secondary); font-size: 0.875rem; margin: 0;">Add data points manually without connecting an API</p>
                    </div>

                    <form id="manualEntryForm" onsubmit="handleManualEntry(event)">
                        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(12rem, 1fr)); gap: 1rem; margin-bottom: 1rem;">
                            <div class="form-group" style="margin-bottom: 0;">
                                <label class="form-label" for="metricCategory">Category</label>
                                <select id="metricCategory" class="form-select" required>
                                    <option value="">Select category...</option>
                                    <option value="finance">💰 Finance</option>
                                    <option value="fitness">🏃 Fitness</option>
                                    <option value="productivity">💻 Productivity</option>
                                    <option value="health">❤️ Health</option>
                                    <option value="mood">😊 Mood</option>
                                    <option value="sleep">😴 Sleep</option>
                                    <option value="custom">🔧 Custom</option>
                                </select>
                            </div>

                            <div class="form-group" style="margin-bottom: 0;">
                                <label class="form-label" for="metricName">Metric Name</label>
                                <input type="text" id="metricName" class="form-input" placeholder="e.g., Coffee Purchase" required>
                            </div>

                            <div class="form-group" style="margin-bottom: 0;">
                                <label class="form-label" for="metricValue">Value</label>
                                <input type="number" step="0.01" id="metricValue" class="form-input" placeholder="e.g., 5.50" required>
                            </div>

                            <div class="form-group" style="margin-bottom: 0;">
                                <label class="form-label" for="metricDate">Date</label>
                                <input type="date" id="metricDate" class="form-input" required>
                            </div>
                        </div>

                        <div class="form-group" style="margin-bottom: 1rem;">
                            <label class="form-label" for="metricNotes">Notes (Optional)</label>
                            <input type="text" id="metricNotes" class="form-input" placeholder="Add a note about this entry...">
                        </div>

                        <button type="submit" class="btn btn-primary">➕ Add Entry</button>
                    </form>
                </div>

                <div class="info-box">
                    <h3>💡 Popular Integrations</h3>
                    <ul>
                        <li>Fitness trackers sync your activity, heart rate, and sleep data</li>
                        <li>Productivity tools monitor your work hours and focus time</li>
                        <li>Finance APIs track spending patterns and savings goals</li>
                        <li>Custom APIs let you bring in any data via REST endpoints</li>
                    </ul>
                </div>

                <!-- Source Cards Grid -->
                <div class="source-grid">
                    <div class="source-card" onclick="openModal('fitbit')">
                        <div class="source-icon">🏃</div>
                        <h3>Fitbit</h3>
                        <p>Track steps, heart rate, calories, and sleep quality from your Fitbit device.</p>
                        <span class="badge badge-popular">Popular</span>
                    </div>

                    <div class="source-card" onclick="openModal('strava')">
                        <div class="source-icon">🚴</div>
                        <h3>Strava</h3>
                        <p>Import running and cycling activities, routes, and performance metrics.</p>
                        <span class="badge badge-popular">Popular</span>
                    </div>

                    <div class="source-card" onclick="openModal('github')">
                        <div class="source-icon">💻</div>
                        <h3>GitHub</h3>
                        <p>Monitor your coding activity, commits, pull requests, and contributions.</p>
                        <span class="badge badge-new">New</span>
                    </div>

                    <div class="source-card" onclick="openModal('googlesheets')">
                        <div class="source-icon">📋</div>
                        <h3>Google Sheets</h3>
                        <p>Pull data from spreadsheets for custom tracking and visualization.</p>
                    </div>

                    <div class="source-card" onclick="openModal('todoist')">
                        <div class="source-icon">✅</div>
                        <h3>Todoist</h3>
                        <p>Track task completion rates, productivity streaks, and project progress.</p>
                    </div>

                    <div class="source-card" onclick="openModal('spotify')">
                        <div class="source-icon">🎵</div>
                        <h3>Spotify</h3>
                        <p>Analyze listening habits, favorite genres, and music discovery trends.</p>
                    </div>

                    <div class="source-card" onclick="openModal('rescuetime')">
                        <div class="source-icon">⏰</div>
                        <h3>RescueTime</h3>
                        <p>Monitor screen time, app usage, and productivity scores automatically.</p>
                    </div>

                    <div class="source-card" onclick="openModal('myfitnesspal')">
                        <div class="source-icon">🍎</div>
                        <h3>MyFitnessPal</h3>
                        <p>Track nutrition, calorie intake, macros, and meal logging data.</p>
                    </div>

                    <div class="source-card" onclick="openModal('custom')">
                        <div class="source-icon">🔧</div>
                        <h3>Custom API</h3>
                        <p>Connect any REST API endpoint to import custom data sources.</p>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <!-- Modal -->
    <div id="configModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title" id="modalTitle">Configure Data Source</h2>
                <button class="close-btn" onclick="closeModal()">&times;</button>
            </div>

            <form id="sourceForm" onsubmit="handleSubmit(event)">
                <div class="form-group">
                    <label class="form-label" for="sourceName">Connection Name</label>
                    <input type="text" id="sourceName" class="form-input" placeholder="My Fitbit Data" required>
                    <div class="form-help">Give this connection a memorable name</div>
                </div>

                <div class="form-group">
                    <label class="form-label" for="apiKey">API Key / Token</label>
                    <input type="password" id="apiKey" class="form-input" placeholder="Enter your API key" required>
                    <div class="form-help">Your credentials are encrypted and stored securely</div>
                </div>

                <div class="form-group">
                    <label class="form-label" for="refreshRate">Data Refresh Rate</label>
                    <select id="refreshRate" class="form-select">
                        <option value="5">Every 5 minutes</option>
                        <option value="15" selected>Every 15 minutes</option>
                        <option value="30">Every 30 minutes</option>
                        <option value="60">Every hour</option>
                        <option value="1440">Once per day</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label" for="dataTypes">Data to Sync</label>
                    <div style="display: flex; flex-direction: column; gap: 0.75rem; margin-top: 0.5rem;">
                        <div class="checkbox-group">
                            <input type="checkbox" id="steps" checked>
                            <label for="steps">Daily Steps</label>
                        </div>
                        <div class="checkbox-group">
                            <input type="checkbox" id="heartrate" checked>
                            <label for="heartrate">Heart Rate</label>
                        </div>
                        <div class="checkbox-group">
                            <input type="checkbox" id="sleep" checked>
                            <label for="sleep">Sleep Data</label>
                        </div>
                        <div class="checkbox-group">
                            <input type="checkbox" id="calories">
                            <label for="calories">Calories Burned</label>
                        </div>
                    </div>
                </div>

                <div class="form-group">
                    <label class="form-label" for="manualData">Manual Data Entry (Optional)</label>
                    <textarea id="manualData" class="form-textarea" placeholder='Enter custom data points in JSON format:
[
  {"date": "2024-10-27", "value": 8500, "metric": "steps"},
  {"date": "2024-10-26", "value": 7200, "metric": "steps"}
]'></textarea>
                    <div class="form-help">Add custom data points manually in JSON format for one-time imports</div>
                </div>

                <div class="form-group">
                    <label class="form-label" for="notes">Notes (Optional)</label>
                    <textarea id="notes" class="form-textarea" placeholder="Add any additional notes about this connection..."></textarea>
                </div>

                <div class="btn-group">
                    <button type="submit" class="btn btn-primary">Connect Source</button>
                    <button type="button" class="btn btn-secondary" onclick="closeModal()">Cancel</button>
                </div>
            </form>
        </div>
    </div>
    <script src="addSource.js"></script>
</body>
</html>